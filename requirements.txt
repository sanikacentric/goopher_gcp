# Minimum-version ranges (not exact pins) so pip resolves a consistent set.

# --- Core runtime ---
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
PyJWT>=2.10
python-multipart>=0.0.20

# --- LLM (OpenAI — ACTIVE: used for all conversations) ---
openai>=1.59

# --- LLM (Gemini) — used when LLM_PROVIDER=gemini ---
google-generativeai>=0.8.3

# --- ADK orchestrator (T2) — Gemini multi-agent path; tools registered in-process ---
google-adk>=1.2

# --- Google Cloud free tier: Firestore + observability (T7, T10) ---
google-cloud-firestore>=2.20
opentelemetry-sdk>=1.29
opentelemetry-exporter-gcp-trace>=1.9

# --- Tests & evals (T8, T9) ---
pytest>=8.3
pytest-asyncio>=0.25
httpx>=0.28
