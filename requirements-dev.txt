# Minimal dependency set to run unit tests & evals WITHOUT the heavy
# ADK / Google Cloud packages. The app degrades gracefully to its
# deterministic template engine when those optional packages/keys are absent,
# so CI validates the core logic fast and offline (no cloud, no LLM key).
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
PyJWT>=2.10
python-multipart>=0.0.20
pytest>=8.3
pytest-asyncio>=0.25
httpx>=0.28
openpyxl>=3.1
